import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container:{
        flexDirection: 'row',
        gap: 16,
    },
    text:{
        fontSize: 16,
        color: '#007AFF',
        fontWeight: '600',
    }
})

export default styles